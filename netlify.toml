[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

[build.environment]
  VITE_API_BASE_URL = "https://your-backend-host.example.com"

[[redirects]]
  from = "/health"
  to = "https://your-backend-host.example.com/health"
  status = 200
  force = true

